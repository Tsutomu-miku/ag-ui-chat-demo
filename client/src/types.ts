/**
 * This file is kept as a thin re-export for backward compatibility.
 * The canonical types now live in the `ag-ui-react` package.
 */

export type {
  ToolCallFunction,
  ChatMessage,
  ChatThread,
  ThreadSummary,
  FrontendToolDefinition,
  PendingToolCall,
  ActiveStep,
  ThreadAgentEvent,
} from "ag-ui-react";
